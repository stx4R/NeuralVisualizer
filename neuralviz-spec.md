# 신경망 학습 시각화 — 설계 스펙 & Claude Code 프롬프트

프로젝트명: **NeuralViz**
목표: 라이브러리 없이 순수 JavaScript로 다층 퍼셉트론을 구현하고, 학습 과정(결정 경계·가중치·손실)을 실시간으로 시각화한다.

---

## 0. 설계 결정 (확정 사항)

| 항목 | 결정 | 이유 |
|---|---|---|
| 빌드 도구 | 없음. `index.html` + ES 모듈 | 프로젝트 본질이 "직접 구현"이라 툴체인이 방해됨 |
| 외부 라이브러리 | 0개. 행렬 연산까지 직접 작성 | 라이브러리 쓰면 프로젝트 의미 소멸 |
| 문제 | 2D 점 이진 분류 | 입력이 2차원이라 결정 경계를 평면에 그릴 수 있음 |
| 데이터셋 | XOR(사분면), 원형, 나선, 가우시안 2군집 | 선형분리 가능/불가능을 대비시켜 은닉층의 필요성을 보여줌 |
| 망 구조 | 입력 2 → 은닉층 가변(기본 [4,4]) → 출력 1 | |
| 활성화 | 은닉: tanh / ReLU / sigmoid 선택, 출력: sigmoid 고정 | |
| 손실 | 이진 교차 엔트로피(BCE) 고정 | sigmoid + BCE 조합에서 출력층 델타가 `a - y`로 단순해짐 |
| 최적화 | 풀배치 경사하강법 | 데이터 300점 규모에서 미니배치·모멘텀·Adam은 불필요한 복잡도 |
| 초기화 | Xavier(tanh/sigmoid), He(ReLU) | 초기화 실패로 학습이 안 되는 상황을 미리 차단 |
| 정밀도 | JS 기본 float64 | 그래디언트 체킹 상대오차 1e-7 기준을 쓸 수 있음 |
| UI 언어 | 한국어 | |

**실행 방법:** ES 모듈은 `file://`에서 CORS로 막히므로 로컬 서버 필요.
`python -m http.server 8000` 후 `http://localhost:8000` 접속.

---

## 0-1. 수식 정의 (구현 기준)

전방 전파 (층 $l$):
```
z[l] = W[l] · a[l-1] + b[l]
a[l] = f(z[l])
```

손실 (샘플 수 m):
```
J = -(1/m) Σ [ y·log(a) + (1-y)·log(1-a) ]
```
`log(0)` 방지를 위해 `a`를 `[1e-12, 1-1e-12]`로 클리핑한다.

역전파:
```
출력층:  δ[L] = a[L] - y                    (sigmoid + BCE 결합으로 도함수가 상쇄됨)
은닉층:  δ[l] = (W[l+1]ᵀ · δ[l+1]) ⊙ f'(z[l])
기울기:  dW[l] = (1/m) · δ[l] · a[l-1]ᵀ
         db[l] = (1/m) · Σ δ[l]
갱신:    W[l] ← W[l] - η · dW[l]
```

활성화 도함수:
```
tanh:    f'(z) = 1 - tanh(z)²
ReLU:    f'(z) = z > 0 ? 1 : 0
sigmoid: f'(z) = σ(z)(1 - σ(z))
```

---

## 1단계 프롬프트 — 코어 엔진 + 검증

> **1단계에서는 시각화 코드를 절대 작성하지 마라. 그래디언트 체킹이 통과하기 전까지 UI/Canvas 작업 금지.**

```
순수 JavaScript(ES 모듈, 외부 라이브러리 0개)로 다층 퍼셉트론의 학습 엔진을 구현해줘.
이번 단계에서는 시각화와 UI를 일절 만들지 마. 엔진과 검증만.

만들 파일:
- src/matrix.js
- src/activations.js
- src/network.js
- src/data.js
- src/gradcheck.js

[src/matrix.js]
2차원 배열(number[][])을 행렬로 쓴다. 클래스 말고 순수 함수로:
matmul(A, B), transpose(A), add(A, B), scale(A, k),
hadamard(A, B), sumRows(A), zeros(r, c), map(A, fn)
차원이 안 맞으면 명확한 메시지와 함께 throw.

[src/activations.js]
tanh, relu, sigmoid 각각에 대해 { fn, derivative, initScale } 형태로 export.
initScale은 가중치 초기화 계수: tanh/sigmoid는 Xavier(sqrt(1/fanIn)),
relu는 He(sqrt(2/fanIn)).

[src/network.js]
class Network { constructor(layerSizes, hiddenActivation) }
- layerSizes 예: [2, 4, 4, 1]. 마지막 층 활성화는 항상 sigmoid.
- 가중치는 정규분포 난수 × initScale, 편향은 0으로 초기화.
- forward(X): 모든 층의 z와 a를 담은 캐시를 반환. X는 열이 샘플인 (feature × m) 행렬.
- loss(A_out, Y): 이진 교차 엔트로피. a를 [1e-12, 1-1e-12]로 클리핑.
- backward(cache, Y): { dW: [], db: [] } 반환.
- step(grads, learningRate): 가중치 갱신.
- getFlatParams() / setFlatParams(arr): 모든 W와 b를 1차원 배열로 직렬화/복원.
  (그래디언트 체킹 전용. 순서는 W[0], b[0], W[1], b[1], ... 로 고정)
- predict(X): 순전파 후 출력층 활성값만 반환.

수식은 반드시 아래를 따를 것:
  출력층 델타 = a - y  (sigmoid + BCE 결합으로 도함수가 상쇄됨)
  은닉층 델타 = (W[l+1]ᵀ · δ[l+1]) ⊙ f'(z[l])
  dW[l] = (1/m) · δ[l] · a[l-1]ᵀ,  db[l] = (1/m) · Σ δ[l]

[src/data.js]
generate(type, count, noise) → { X, Y }
X는 (2 × count) 행렬, Y는 (1 × count) 행렬, 라벨은 0 또는 1.
type: 'xor' | 'circle' | 'spiral' | 'gaussian'
- xor: [-1,1]² 균등 분포, 1·3사분면이 1, 2·4사분면이 0
- circle: 원점 기준 반지름 0.5 안쪽이 1, 바깥 고리가 0
- spiral: 두 갈래 아르키메데스 나선, 각 갈래가 서로 다른 클래스
- gaussian: (0.5, 0.5)와 (-0.5, -0.5) 중심의 두 가우시안 군집
noise는 0~0.5, 좌표에 더할 가우시안 잡음의 표준편차.
모든 좌표는 대략 [-1, 1] 범위에 들어오게 할 것.
재현성을 위해 시드 고정 가능한 간단한 의사난수 생성기(mulberry32 등)를 쓸 것.

[src/gradcheck.js]
checkGradients(network, X, Y, epsilon = 1e-4) 함수.
- backward로 얻은 해석적 기울기를 평탄화한다.
- 각 파라미터 θᵢ에 대해 (J(θᵢ+ε) - J(θᵢ-ε)) / (2ε) 로 수치 기울기를 구한다.
- 두 벡터의 상대오차 ‖a-n‖ / (‖a‖+‖n‖) 를 계산한다.
- { relativeError, passed, worstIndex, analytic, numeric } 반환. passed 기준은 1e-7 미만.

완료 검증:
tests/run.html 을 만들어 브라우저에서 열면 아래 4가지가 콘솔에 출력되게 할 것.
1. [2,4,4,1] tanh 망 + xor 데이터 200개로 그래디언트 체킹 → 상대오차와 통과 여부
2. relu, sigmoid 은닉 활성화에 대해서도 각각 그래디언트 체킹 → 통과 여부
3. [2,1] 망(은닉층 없음)으로 xor 학습 2000 epoch → 손실이 0.69 근처에서 안 내려가는 것 확인
4. [2,4,4,1] 망으로 xor 학습 2000 epoch, 학습률 0.5 → 손실 0.1 미만, 정확도 95% 이상

4번이 통과할 때까지 다음 단계로 넘어가지 마.
```

---

## 2단계 프롬프트 — 시각화

```
1단계에서 만든 엔진 위에 Canvas 2D 시각화를 얹어줘. 여전히 외부 라이브러리는 쓰지 마.
엔진 파일(matrix, activations, network, data, gradcheck)은 수정하지 마.
수정이 필요하다고 판단되면 먼저 이유를 말하고 물어봐.

만들 파일:
- src/viz-boundary.js
- src/viz-network.js
- src/viz-loss.js

[src/viz-boundary.js] 결정 경계 히트맵
drawBoundary(canvas, network, data)
- 캔버스 좌표계는 [-1.2, 1.2]² 를 매핑. 정사각형.
- 60×60 그리드의 모든 점을 한 번의 순전파로 predict (3600개를 행렬 하나로 묶어 보낼 것.
  점마다 forward를 호출하지 말 것).
- 출력값 0~1을 색으로: 0은 주황(#e8853a), 0.5는 흰색, 1은 파랑(#3a7ae8). 선형 보간.
- ImageData로 60×60 픽셀을 만든 뒤 오프스크린 캔버스에 넣고 imageSmoothingEnabled=true 로
  확대해 그린다. 픽셀마다 fillRect 하지 말 것.
- 그 위에 데이터 점을 그린다. 반지름 4px, 라벨 1은 파랑 채움, 라벨 0은 주황 채움,
  둘 다 흰색 1px 테두리.

[src/viz-network.js] 망 다이어그램
drawNetwork(canvas, network)
- 층을 왼쪽에서 오른쪽으로 배치, 각 층의 뉴런은 세로 균등 배치. 뉴런은 반지름 12px 원.
- 가중치는 뉴런을 잇는 직선. 선 두께 = clamp(|w| × 1.5, 0.5, 6) px.
  색은 양수 파랑(#3a7ae8), 음수 주황(#e8853a). 투명도 = clamp(|w|/3, 0.15, 1).
- 편향은 각 뉴런 원의 채움 색으로 표현 (같은 파랑/주황 스케일).
- 층 아래에 라벨: '입력', '은닉 1', '은닉 2', '출력'.

[src/viz-loss.js] 손실 곡선
drawLoss(canvas, history)
- history는 epoch별 손실값 배열. x축은 epoch, y축은 손실.
- y축은 0부터 시작하고 최댓값에 맞춰 자동 스케일. 축과 눈금 3개, 마지막 손실값을 우상단에 텍스트로.
- 점이 캔버스 폭보다 많아지면 균등 간격으로 다운샘플링해서 그린다.

세 함수 모두 부수효과 없이 매 호출마다 캔버스를 지우고 다시 그리는 형태로.
전역 상태를 두지 말 것.

완료 검증:
임시 데모 페이지에서 학습된 [2,4,4,1] 망과 xor 데이터를 넣었을 때
세 캔버스가 각각 의도대로 그려지는지 눈으로 확인.
```

---

## 3단계 프롬프트 — UI와 학습 루프

```
index.html, style.css, src/main.js 를 만들어서 전체를 하나로 묶어줘.
엔진과 시각화 모듈은 수정하지 마.

레이아웃 (데스크톱 기준, 최소 폭 1000px 가정):
- 왼쪽: 결정 경계 캔버스 (정사각형, 400×400)
- 오른쪽 위: 망 다이어그램 캔버스 (400×280)
- 오른쪽 아래: 손실 곡선 캔버스 (400×160)
- 최상단: 컨트롤 바

컨트롤:
- 데이터셋 선택 (XOR / 원형 / 나선 / 가우시안) — 변경 시 데이터 재생성 + 망 초기화
- 노이즈 슬라이더 0~0.5, 0.05 단위
- 은닉층 구성: 층 추가/제거 버튼, 각 층의 뉴런 수 +/- (1~8) — 변경 시 망 초기화
- 은닉 활성화 선택 (tanh / ReLU / sigmoid) — 변경 시 망 초기화
- 학습률 슬라이더: 0.01 ~ 3.0, 로그 스케일
- 재생 / 일시정지 / 1스텝 / 초기화 버튼
- 개발자용: '그래디언트 검증' 버튼 — checkGradients 실행 후 상대오차와 통과 여부를 화면에 표시

상태 표시: 현재 epoch, 현재 손실(소수점 4자리), 학습 데이터 정확도(%)

학습 루프:
- requestAnimationFrame 사용.
- 한 프레임당 학습 스텝 수는 상수 STEPS_PER_FRAME = 3 으로 두고 파일 상단에 선언.
- 손실 곡선과 상태 표시는 매 프레임 갱신.
- 결정 경계와 망 다이어그램은 5프레임마다 갱신 (프레임 카운터로).
- 일시정지 중에도 캔버스는 마지막 상태를 유지.

스타일: 다크 배경(#1a1a1e), 캔버스 배경은 밝게, 산세리프, 장식 최소화.
데이터 색(파랑 #3a7ae8 / 주황 #e8853a)이 UI에서 다른 의미로 쓰이지 않게 할 것.

완료 검증:
1. 나선 데이터 + 은닉층 [6,6] + tanh + 학습률 0.5 로 결정 경계가 나선을 따라 휘는 것 확인
2. 학습률을 3.0으로 올리면 손실이 발산하거나 진동하는 것 확인
3. 은닉층을 모두 제거하면 XOR에서 결정 경계가 직선 하나에 머무는 것 확인
4. '그래디언트 검증' 버튼이 어떤 설정에서도 통과로 나오는 것 확인
```

---

## 4단계 프롬프트 — 마무리

```
아래 항목만 처리해줘. 새 기능은 추가하지 마.

1. README.md 작성
   - 프로젝트 개요 3~4줄
   - 실행 방법 (python -m http.server)
   - 구현한 수식: 순전파, BCE 손실, 역전파 델타 규칙, 갱신식을 수식으로 명시
   - 그래디언트 체킹 설명: 왜 필요한지, 어떻게 검증하는지, 실제 측정된 상대오차 값
   - 파일별 역할 한 줄씩
   - 실험 관찰 3가지: 은닉층 유무와 XOR, 학습률과 발산, 활성화 함수별 수렴 속도 차이
   외부 라이브러리를 쓰지 않았다는 점을 명시할 것.

2. 코드 정리
   - 사용하지 않는 함수·변수 제거
   - 핵심 수식이 나오는 지점(역전파 델타 계산, 그래디언트 체킹)에만 주석.
     자명한 코드에는 주석 달지 말 것.

3. 재현 가능한 프리셋
   README에 스크린샷/GIF 촬영용 설정 3개를 표로 정리:
   (a) XOR + [4,4] + tanh + lr 0.5
   (b) 나선 + [6,6] + tanh + lr 0.3
   (c) 원형 + 은닉층 없음 + lr 0.5  ← 실패 사례
```

---

## 진행 체크리스트

- [ ] 1단계: 그래디언트 체킹 상대오차 < 1e-7 (tanh/ReLU/sigmoid 모두)
- [ ] 1단계: 은닉층 없는 망이 XOR에서 손실 0.69 근처에 머무름
- [ ] 1단계: [2,4,4,1] 망이 XOR에서 정확도 95% 이상
- [ ] 2단계: 세 캔버스가 의도대로 렌더링
- [ ] 3단계: 나선 데이터에서 결정 경계가 나선을 따라 휨
- [ ] 3단계: 높은 학습률에서 발산 재현
- [ ] 4단계: README에 수식과 실측 상대오차 기록
- [ ] GIF 3종 촬영 (프리셋 a/b/c)

## 확장 후보 (지금은 하지 말 것)

핵심이 완성된 뒤에만 고려. 미리 만들면 코어가 부실해진다.

- 은닉 뉴런별 활성값을 작은 히트맵으로 표시 (특징이 어떻게 조합되는지 보여줌)
- 학습률 스케줄링 비교
- L2 정규화 on/off 비교 (노이즈 높은 데이터에서 과적합 시연)
- 학습 과정을 epoch 단위로 되감는 타임라인 슬라이더
